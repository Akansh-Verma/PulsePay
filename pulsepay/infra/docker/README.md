# Docker (pulsepay path)

Canonical Compose and Dockerfiles live at **`../../infra/docker/`** (repository root).

- Compose: `../../infra/docker/docker-compose.yml`
- Level 1 doc: `../../docs/LEVEL1.md`

The `docker-compose.yml` in this directory mirrors that stack and uses the same build context (repository root).

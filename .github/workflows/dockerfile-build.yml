name: Dockerfile-style Maven build verification

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  dockerfile-reactor-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Cache local Maven repository
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Prepare reactor build context
        run: |
          # Mirror the Dockerfile build context: reactor-pom.xml becomes pom.xml
          # and service parent relativePath values resolve to ../../pom.xml.
          mkdir -p infra/docker/services
          cp infra/docker/reactor-pom.xml infra/docker/pom.xml
          cp -R services/payment-service infra/docker/services/
          cp -R services/ledger-service infra/docker/services/

      - name: Build payment-service (Dockerfile-style)
        run: mvn -f infra/docker/pom.xml -B -pl services/payment-service package -DskipTests

      - name: Build ledger-service (Dockerfile-style)
        run: mvn -f infra/docker/pom.xml -B -pl services/ledger-service package -DskipTests
